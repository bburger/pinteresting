- if user_signed_in?
	%p#intro
		Hello
		= current_user.name
		%br/
		%span
			Share what you've found and see what other's have found!
- else
	%p#intro
		There's a lot of cool stuff out there.
		%br/
		%span
			Share what you've found and see what other's have found!	
#posts
	- @posts.each do |post|
		.post
			.post_image
				= link_to (image_tag post.image.url(:small)), post
			.post_content
				.title
					%h2= link_to post.title, post
				.data.clearfix
					%p.username
						Shared by
						= post.user.name
					%p.buttons
						%span
							%i.fa.fa-comments-o
							= post.comments.count
						%span
							%i.fa.fa-thumbs-o-up
							= post.get_likes.size